schemaVersion: 2.2.0
attributes:
    space:
      # regular, large, xlarge
      instanceType: regular
      editor:
        type: Idea
    warmup:
        # run every day at 4:00 and 12:00
        startOn:
            - type: schedule
            cron: '0 4,12 * * *'
